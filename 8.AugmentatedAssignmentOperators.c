#include<stdio.h>

int main(){
    
    //Augmented assignment operators = used to replace a statement where an
    //operator takes a vriable as one of its arguments and then assigns the result 
    //back to the same variable.
    //x = x + 1
    //x+=1

    int x = 10;
    x = x + 1;
    //or
    x+=1;

    x = x  *4;
    //or
    x*=4;

    //e,t,c

    return 0;
}
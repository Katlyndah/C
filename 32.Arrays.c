#include<stdio.h>

int main(){
    
    //array = a data structure that can store many values of the same data type

    double prices[] = {5.0, 10.0, 15.0, 25.0, 20.0};

    printf("$%.2lf", prices[3]); //to access values in the array

    //char name[] = "Kat";
    //They have fixed sizes.
    //you can set the array size double prices[5] = {10, 11};
    //values can be added later in the program
 
    return 0;
}
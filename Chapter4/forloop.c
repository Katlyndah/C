#include <stdio.h>

int main()
{
    // for loop
    // for(initialize; test; increment/decrement)
    // loop is terminated when test/condition is nolonger true
    for (int i = 0; i < 7; i++)
    {
        printf("The value of i is %d\n", i);
        //or printf("\n"); for new line
    }

    return 0;
}
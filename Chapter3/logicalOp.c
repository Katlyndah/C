#include<stdio.h>

int main(){
    int a = 34;
    int b = 5; //try with true | && means 'and' 
    if(a>7 && b<4){
        printf("yes"); //Only happens when both statements are true
    }
    return 0;
}